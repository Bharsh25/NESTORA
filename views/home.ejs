<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Nestora | Find Your Perfect Stay</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', sans-serif;
        }

        body {
            background-color: #f7f8fa;
            color: #333;
        }

        /* Navbar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 3rem;
            background-color: #fff;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            color: #ff5a5f;
            letter-spacing: 1px;
        }

        nav a {
            margin-left: 1.5rem;
            text-decoration: none;
            color: #333;
            font-weight: 500;
            transition: color 0.2s;
        }

        nav a:hover {
            color: #ff5a5f;
        }

        /* Hero Section */
        .hero {
            background: url('https://images.unsplash.com/photo-1505691938895-1758d7feb511?auto=format&fit=crop&w=1600&q=80') center/cover no-repeat;
            height: 90vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
            position: relative;
        }

        .hero::before {
            content: '';
            position: absolute;
            inset: 0;
            background: rgba(0,0,0,0.4);
        }

        .hero-text {
            position: relative;
            z-index: 2;
            max-width: 700px;
            padding: 0 1rem;
        }

        .hero-text h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .hero-text p {
            font-size: 1.2rem;
            margin-bottom: 2rem;
        }

        .btn-primary {
            background-color: #ff5a5f;
            color: #fff;
            border: none;
            padding: 0.8rem 2rem;
            font-size: 1rem;
            border-radius: 30px;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #e1494f;
        }

        /* Footer */
        footer {
            background-color: #fff;
            text-align: center;
            padding: 1rem;
            font-size: 0.9rem;
            color: #666;
            border-top: 1px solid #eee;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .navbar {
                flex-direction: column;
                padding: 1rem;
            }

            nav a {
                margin: 0.5rem;
                display: inline-block;
            }

            .hero-text h1 {
                font-size: 2.2rem;
            }

            .hero-text p {
                font-size: 1rem;
            }
        }
    </style>
</head>

<body>
    <header class="navbar">
        <div class="logo">Nestora</div>
        <nav>
            <a href="#">Home</a>
            <a href="/listings">Explore</a>
            <form method="get" action="/listings/new" style="display:inline;">
                <a href="/listings/new">Host</a>
            </form>
            
            <a href="/login">Login</a>
        </nav>
    </header>

    <section class="hero">
        <div class="hero-text">
            <h1>Find Your Perfect Stay, Anywhere</h1>
            <p>Discover cozy homes, luxury villas, and unique stays around the world.</p>
            <button class="btn-primary">Start Exploring</button>
        </div>
    </section>

    <footer>
        <p>© 2025 Nestora. All rights reserved.</p>
    </footer>
</body>
</html>
